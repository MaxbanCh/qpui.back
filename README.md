# QPUI Backend

The rust backend has for role to keep track of rooms data and send messages to all the clients.

## What is implemented

- Room can be created
- Different users can join one room

## How to start project ?

Add all dependancies with
```bash
cargo add [dependancy]
```

Then run

```bash
cargo run
```